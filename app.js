'use strict';
const $ = s => document.querySelector(s);
const esc = s => String(s ?? '').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const normalize = Romaji.searchKey;
const defaults={kana:true,romaji:true,fr:true,font:'serif',size:28,rate:.86,gap:2000,voice:'',lesson:'N1'};
let settings={...defaults}, favorites=new Set(), lessons=[], vocab=[], grammar=[], lessonIds=[];
try{settings={...defaults,...JSON.parse(localStorage.getItem('nihongo-settings')||'{}')};favorites=new Set(JSON.parse(localStorage.getItem('nihongo-favorites')||'[]'));}catch{}
let audioRun=0,audioTimer,voices=[],currentUtterance;const speech=window.speechSynthesis;
let search='',onlyFavorites=false,page=0;const pageSize=24;const answers=new Map();
let decorticage, decorticageData;
function save(){try{localStorage.setItem('nihongo-settings',JSON.stringify(settings));localStorage.setItem('nihongo-favorites',JSON.stringify([...favorites]));}catch{$('#audio-status').textContent='Le stockage local est indisponible : vos réglages restent actifs pour cette visite.';}}
function applySettings(){for(const k of ['kana','romaji','fr']){document.body.classList.toggle('hide-'+k,!settings[k]);$('#show-'+k).checked=!!settings[k];}document.body.classList.toggle('rounded',settings.font==='rounded');document.documentElement.style.setProperty('--jp-size',settings.size+'px');for(const k of ['font','size','rate','gap'])$('#'+k).value=settings[k];$('#size-value').textContent=settings.size+' px';$('#rate-value').textContent=Number(settings.rate).toFixed(2)+' ×';}
function updateVoices(){voices=speech?speech.getVoices().filter(v=>/^ja(?:-|_)/i.test(v.lang)||v.lang==='ja'):[];$('#voice').innerHTML='<option value="">Automatique · préférence japonaise</option>'+voices.map(v=>`<option value="${esc(v.voiceURI)}">${esc(v.name)}</option>`).join('');$('#voice').value=settings.voice;$('#voice').disabled=!voices.length;if(!speech)$('#audio-status').textContent='La synthèse vocale n’est pas disponible dans ce navigateur.';}
function stopAudio(message=''){audioRun++;clearTimeout(audioTimer);speech?.cancel();currentUtterance=null;$('#stop-audio').disabled=true;$('#audio-status').textContent=message;document.querySelectorAll('.speaking').forEach(x=>x.classList.remove('speaking'));}
function speak(items){stopAudio();if(!speech){$('#audio-status').textContent='Audio indisponible dans ce navigateur.';return;}const run=audioRun;let index=0;$('#stop-audio').disabled=false;function next(){if(run!==audioRun)return;if(index>=items.length){stopAudio('Écoute terminée.');return;}const text=items[index++];const u=new SpeechSynthesisUtterance(text);currentUtterance=u;u.lang='ja-JP';u.rate=Number(settings.rate);u.voice=voices.find(v=>v.voiceURI===settings.voice)||voices.find(v=>/kyoko|hana|haruka|mizuki|female/i.test(v.name))||voices[0]||null;$('#audio-status').textContent=`Écoute ${index} / ${items.length} · ${u.voice?.name||'voix du navigateur'}`;u.onend=()=>{if(run===audioRun)audioTimer=setTimeout(next,index<items.length?Number(settings.gap):0);};u.onerror=e=>{if(run===audioRun&&e.error!=='canceled'&&e.error!=='interrupted')stopAudio('Lecture impossible. Essayez une autre voix japonaise dans les réglages.');};speech.speak(u);}next();}
function audioButton(text){return `<button class="audio" data-speak="${esc(text)}" aria-label="Écouter en japonais">▶ Écouter</button>`;}
function star(id){return `<button class="favorite" data-favorite="${esc(id)}" aria-pressed="${favorites.has(id)}" aria-label="${favorites.has(id)?'Retirer des':'Ajouter aux'} favoris">${favorites.has(id)?'★':'☆'}</button>`;}
function jp(text){return esc(text).replace(/\p{Script=Han}+/gu,'<span class="kanji">$&</span>');}
function block(t,{audio=true,fr=true}={}) {
 const sameText = String(t.jp).normalize('NFC').replace(/\s/g, '') === String(t.kana).normalize('NFC').replace(/\s/g, '');
 const japanese = sameText ? '<span class="kana-idem" lang="fr" title="Identique au texte kana">---</span>' : jp(t.jp);
 return `<div class="language-block"><button type="button" class="kana kana-audio" lang="ja" data-speak="${esc(t.audioKana || t.kana)}" aria-label="Écouter : ${esc(t.kana)}">${esc(t.kana)}<span class="sound-note" aria-hidden="true"> ♪</span></button><p class="jp" lang="ja">${japanese}</p><p class="romaji">${esc(Romaji.display(t.romaji))}</p>${fr?`<p class="fr">${esc(t.fr)}</p>`:''}${audio?audioButton(t.kana):''}</div>`;
}
function intro(k,title,description){return `<div class="intro"><div class="eyebrow">${k}</div><h1>${esc(title)}</h1><p>${esc(description)}</p></div>`;}
function route(){const [tab='lecons',id,line]=location.hash.slice(1).split('/');return {tab:['lecons','dictionnaire','grammaire','atelier'].includes(tab)?tab:'lecons',id:decodeURIComponent(id||''),line};}
function render(){stopAudio();const r=route();document.querySelectorAll('[data-tab]').forEach(x=>{if(x.dataset.tab===r.tab)x.setAttribute('aria-current','page');else x.removeAttribute('aria-current');});if(r.tab==='lecons')renderLessons(r);else if(r.tab==='dictionnaire')renderDictionary(r);else if(r.tab==='atelier')renderAtelier(r);else renderGrammar(r);}
function renderLessons(r){const id=lessonIds.includes(r.id)?r.id:lessonIds.includes(settings.lesson)?settings.lesson:lessonIds[0];settings.lesson=id;save();const rows=lessons.filter(x=>x.Leçon===id);const index=lessonIds.indexOf(id);const related=LessonLinks.find(rows,grammar);$('#main').innerHTML=intro('ÉCOUTER · LIRE · RÉPÉTER','Les leçons','Retrouvez les dialogues, une phrase après l’autre.')+`<div class="toolbar"><button data-lesson="${lessonIds[index-1]||''}" ${index===0?'disabled':''} aria-label="Leçon précédente">←</button><label for="lesson-select">Leçon</label><select id="lesson-select">${lessonIds.map(n=>`<option value="${n}" ${n===id?'selected':''}>${esc(lessonLabel(n))}</option>`).join('')}</select><button data-lesson="${lessonIds[index+1]||''}" ${index===lessonIds.length-1?'disabled':''} aria-label="Leçon suivante">→</button><button class="audio" id="play-lesson">▶ Écouter la leçon</button><span class="muted">${rows.length} phrases</span></div><div class="lesson-layout"><div class="stack">${rows.map(l=>`<article class="card" id="${l.Ligne}"><div class="card-head"><span class="number">${id} · ${l.Ligne}</span><div class="card-actions">${star(id+'-'+l.Ligne)}${audioButton(l.Kana)}</div></div>${block({jp:l.Japonais,kana:l.Kana,romaji:l.Romaji,fr:l.Français},{audio:false})}${decorticageEntry(l)}</article>`).join('')}</div><aside class="panel aside"><div class="eyebrow">AUTOUR DE LA LEÇON</div><h3>Pratiquer</h3><a href="#dictionnaire/${id}">Voir le vocabulaire de cette leçon →</a><a href="#atelier/${id}/lesson">M’entraîner sur cette leçon →</a><a href="#atelier/${id}/through">Réviser jusqu’à cette leçon →</a><h3>Grammaire à retrouver</h3>${lessonGrammarLinks(related)}<p class="muted">Écoutez, puis répétez à voix haute. La pause entre les phrases se règle dans les réglages.</p></aside></div>`;$('#lesson-select').onchange=e=>location.hash='lecons/'+e.target.value;$('#play-lesson').onclick=()=>speak(rows.map(l=>l.Kana));if(r.line){const el=document.getElementById(r.line);el?.classList.add('highlight');el?.scrollIntoView({block:'center'});}}
function sourceLink(source){const m=source?.match(/(N\d+)-(S\d+)/);return m?`<a href="#lecons/${m[1]}/${m[2]}">${esc(m[1].replace('N','Leçon '))} · ${m[2]}</a>`:esc(source);}
function baseForm(v){
 const base = vocab.find(entry => entry.mot === v.forme_base);
 return base ? `<details><summary>Forme de dictionnaire</summary>${block({jp:base.mot,kana:base.kana,romaji:base.romaji,fr:base.fr})}</details>` : '';
}
function vocabCard(v){return `<article class="card dictionary-card" id="${v.id}"><div class="card-head"><span class="number">${esc(v.type||'VOCABULAIRE')}</span><div class="card-actions">${star(v.id)}${audioButton(v.kana)}</div></div>${block({jp:v.mot,kana:v.kana,romaji:v.romaji,fr:v.fr},{audio:false})}${v.forme_base?baseForm(v):''}${v.note?`<p class="note">${esc(v.note)}</p>`:''}<details><summary>Exemple dans la leçon</summary>${block(v.exemple)}<p class="muted">${sourceLink(v.source)}</p></details></article>`;}
function renderDictionary(r){const selected=r.id.startsWith('vocab-')?vocab.find(v=>v.id===r.id):null;const filter=lessonIds.includes(r.id)?r.id:'';$('#main').innerHTML=intro('COMPRENDRE · RETROUVER','Le dictionnaire','Le sens des mots, leurs formes et leurs exemples en contexte.')+`<div class="toolbar"><input id="vocab-search" type="search" placeholder="Un mot en japonais, romaji ou français…" aria-label="Rechercher dans le dictionnaire" value="${esc(search)}"><select id="vocab-lesson" aria-label="Filtrer par leçon"><option value="">Toutes les leçons</option>${lessonIds.map(n=>`<option value="${n}" ${filter===n?'selected':''}>${esc(lessonLabel(n))}</option>`).join('')}</select><label><input type="checkbox" id="favorites-only" ${onlyFavorites?'checked':''}> Mes favoris</label></div><p class="muted" id="result-count" role="status"></p><div id="vocab-results" class="grid"></div><div id="pagination"></div>`;function results(){let list=selected?[selected]:vocab.filter(v=>(!filter||[v.source,...(v.exemples_supplementaires||[]).map(e=>e.source)].some(s=>s?.includes(filter+'-')))&&(!onlyFavorites||favorites.has(v.id))&&(!search||normalize([v.mot,v.kana,v.romaji,v.fr,v.forme_base].join(' ')).includes(normalize(search))));const max=Math.max(0,Math.ceil(list.length/pageSize)-1);page=Math.min(page,max);$('#result-count').textContent=`${list.length} mot${list.length>1?'s':''}${selected?' · accès depuis une fiche':''}`;$('#vocab-results').innerHTML=list.length?list.slice(page*pageSize,(page+1)*pageSize).map(vocabCard).join(''):'<p class="panel empty">Aucun mot trouvé. Essayez un autre terme ou retirez un filtre.</p>';$('#pagination').innerHTML=selected?'<a href="#dictionnaire">← Tout le dictionnaire</a>':`<div class="pager"><button id="prev-page" ${page===0?'disabled':''}>← Précédent</button><span>${page+1} / ${max+1}</span><button id="next-page" ${page===max?'disabled':''}>Suivant →</button></div>`;if(!selected){$('#prev-page').onclick=()=>{page--;results();$('#vocab-search').scrollIntoView();};$('#next-page').onclick=()=>{page++;results();$('#vocab-search').scrollIntoView();};}}$('#vocab-search').oninput=e=>{search=e.target.value;page=0;if(selected){location.hash='dictionnaire';}else results();};$('#favorites-only').onchange=e=>{onlyFavorites=e.target.checked;page=0;if(selected)location.hash='dictionnaire';else results();};$('#vocab-lesson').onchange=e=>{page=0;location.hash='dictionnaire/'+e.target.value;};results();}
function renderGrammar(r){const f=grammar.find(f=>f.id===r.id);if(!f){$('#main').innerHTML=intro('OBSERVER · COMPRENDRE · PRATIQUER','La grammaire','20 fiches progressives pour comprendre les phrases de vos leçons.')+`<div class="grid">${grammar.map(f=>`<article class="card grammar-card"><div class="card-head"><span class="pill">${f.id} · ${esc(f.famille)}</span>${star(f.id)}</div><h2>${esc(f.titre)}</h2><p>${esc(f.objectif)}</p><a href="#grammaire/${f.id}">Ouvrir la fiche →</a></article>`).join('')}</div>`;return;}$('#main').innerHTML=`<div class="grammar-detail"><p><a href="#grammaire">← Les 20 fiches</a></p>${intro(f.id+' · '+esc(f.famille),f.titre,f.objectif)}<div class="toolbar">${star(f.id)}${f.prerequis.length?`<span class="muted">Avant cette fiche : ${f.prerequis.map(id=>`<a href="#grammaire/${id}">${id}</a>`).join(', ')}</span>`:''}</div><section class="panel"><h2>Comprendre</h2><p>${esc(f.explication)}</p>${f.constructions.map(t=>block(t,{audio:false})).join('')}<h3>Comment construire la phrase</h3>${f.formation.map(p=>`<p>${esc(p.texte)}</p>${p.formes.map(t=>`<div class="example-block">${block(t)}</div>`).join('')}`).join('')}</section><section class="panel"><h2>Les pièges à éviter</h2><ul>${f.pieges.map(p=>`<li>${esc(p)}</li>`).join('')}</ul></section><section class="panel"><h2>Dans vos leçons</h2>${f.exemples.map(e=>`<div class="example-block">${sourceLink(e.source)}${block(e)}<details><summary>Repérer la construction</summary>${block(e.cible)}<p>${esc(e.commentaire)}</p></details></div>`).join('')}</section><section class="panel"><span class="pill">À VOUS DE JOUER</span><h2>Un petit exercice</h2><p>${esc(f.exercice.consigne)}</p><div id="choices">${f.exercice.options.map(o=>`<div class="choice">${block(o,{audio:false,fr:false})}<button class="choose-answer" data-answer="${o.id}" aria-pressed="false">Choisir cette réponse</button></div>`).join('')}</div><div id="feedback" role="status"></div></section>${f.pour_aller_plus_loin?`<section class="panel"><h2>Pour aller plus loin</h2><p>${esc(f.pour_aller_plus_loin)}</p></section>`:''}<section class="panel"><h2>Les mots de cette fiche</h2><div class="links">${f.vocabulaire.map(v=>`<a href="#dictionnaire/${v.id}"><span lang="ja">${esc(v.mot)}</span><span class="romaji"> · ${esc(Romaji.display(v.romaji))}</span><span class="fr"> · ${esc(v.fr)}</span></a>`).join('')}</div><details><summary>Références pédagogiques</summary>${f.sources.map(s=>`<p><a href="${esc(s.url)}" target="_blank" rel="noopener">${esc(s.organisme)} · ${esc(s.titre)}</a><br><small>${esc(s.section||'')}</small></p>`).join('')}</details></section><div class="pager">${f.ordre>1?`<a href="#grammaire/${grammar[f.ordre-2].id}">← Fiche précédente</a>`:''}${f.ordre<grammar.length?`<a href="#grammaire/${grammar[f.ordre].id}">Fiche suivante →</a>`:''}</div></div>`;function answer(id){answers.set(f.id,id);document.querySelectorAll('[data-answer]').forEach(b=>b.setAttribute('aria-pressed',b.dataset.answer===id));const correct=id===f.exercice.bonne_reponse;const solution=f.exercice.options.find(o=>o.id===f.exercice.bonne_reponse);$('#feedback').innerHTML=`<div class="feedback ${correct?'':'error'}"><strong>${correct?'Bonne réponse !':'La bonne réponse est : '}</strong>${block(solution)}<p>${esc(f.exercice.explication)}</p><details><summary>Traduction des autres propositions</summary>${f.exercice.options.filter(o=>o!==solution).map(o=>block(o)).join('')}</details></div>`;}document.querySelectorAll('[data-answer]').forEach(b=>b.onclick=()=>answer(b.dataset.answer));if(answers.has(f.id))answer(answers.get(f.id));}
$('#settings-toggle').onclick=()=>{const open=$('#settings').hidden;$('#settings').hidden=!open;$('#settings-toggle').setAttribute('aria-expanded',open);};for(const k of ['kana','romaji','fr'])$('#show-'+k).onchange=e=>{settings[k]=e.target.checked;applySettings();save();};for(const k of ['font','size','rate','gap'])$('#'+k).oninput=e=>{settings[k]=['size','rate','gap'].includes(k)?Number(e.target.value):e.target.value;applySettings();save();};$('#voice').onchange=e=>{settings.voice=e.target.value;save();};$('#stop-audio').onclick=()=>stopAudio('Lecture arrêtée.');document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.dataset.speak)speak([b.dataset.speak]);if(b.dataset.lesson)location.hash='lecons/'+b.dataset.lesson;if(b.dataset.favorite){const id=b.dataset.favorite;favorites.has(id)?favorites.delete(id):favorites.add(id);save();b.outerHTML=star(id);renderFavorites();if(onlyFavorites&&route().tab==='dictionnaire')renderDictionary(route());}});window.addEventListener('hashchange',()=>{page=0;render();if(!route().line)window.scrollTo(0,0);});window.addEventListener('pagehide',()=>stopAudio());speech?.addEventListener('voiceschanged',updateVoices);applySettings();updateVoices();
Promise.all(['leconsJap.json','dicoLeconsJap.json','grammaireLeconsJap.json','decorticage.json'].map(async url=>{const r=await fetch(url);if(!r.ok)throw new Error(url);return r.json();})).then(([l,v,g,d])=>{decorticageData=d;decorticage=Decorticage.create(v,d);lessons=l;vocab=v;grammar=g.fiches;lessonIds=[...new Set(l.map(x=>x.Leçon))].sort((a,b)=>Number(a.slice(1))-Number(b.slice(1)));render();renderFavorites();}).catch(()=>{$('#main').innerHTML='<div class="panel"><h1>Le carnet n’a pas pu se charger</h1><p>Ouvrez le site depuis son adresse Web ou avec le serveur local indiqué dans le fichier README, puis réessayez.</p><button onclick="location.reload()">Réessayer</button></div>';});

// Retrouver les favoris de tous les modules sans quitter la lecture en cours.
function renderFavorites() {
 const entries = [...favorites].map(id => {
  const word = vocab.find(v => v.id === id);
  if (word) return `<a href="#dictionnaire/${word.id}">Mot : <span class="romaji">${esc(Romaji.display(word.romaji))} · </span><span class="fr">${esc(word.fr)}</span></a>`;
  const card = grammar.find(f => f.id === id);
  if (card) return `<a href="#grammaire/${id}">${id} · ${esc(card.titre)}</a>`;
  const [lesson,line] = id.split('-');
  const phrase = lessons.find(l => l.Leçon === lesson && l.Ligne === line);
  if (phrase) return `<a href="#lecons/${lesson}/${line}">${lesson} · ${line}<span class="romaji"> · ${esc(Romaji.display(phrase.Romaji))}</span><span class="fr"> · ${esc(phrase.Français)}</span></a>`;
  return '';
 }).filter(Boolean);
 $('#saved-list').innerHTML = entries.length ? entries.join('') : '<p class="muted">Touchez une étoile pour retrouver ici un mot, une phrase ou une fiche.</p>';
}
$('#saved-toggle').onclick = () => {
 const open = $('#saved').hidden;
 $('#saved').hidden = !open;
 $('#saved-toggle').setAttribute('aria-expanded', open);
};
$('#saved-list').addEventListener('click', e => {
 if (e.target.closest('a')) {$('#saved').hidden=true;$('#saved-toggle').setAttribute('aria-expanded',false);}
});


// Analyse à la demande : règles générales et exemples annotés.
function decorticageEntry(row) {
 const id = `${row.Leçon}-${row.Ligne}`;
 if (!decorticage || row.Ligne === 'S00') return '';
 return `<details class="sentence-analysis" data-analysis="${id}"><summary>Décortiquer cette phrase</summary><div class="analysis-content"></div></details>`;
}
function renderDecorticage(row) {
 const result = decorticage.analyze(row);
 if (!result || result.stale) return '<p>Cette phrase a changé : son décorticage doit être revérifié.</p>';
 const labels = {rule:'Règle réutilisable',dictionary:'Dictionnaire',annotation:'Précision de contexte',unknown:'À compléter'};
 return `<div class="analysis-heading"><h3>La phrase, morceau par morceau</h3><span class="analysis-state">${result.partial ? 'Décorticage partiel' : (result.literalOrigin === 'automatic' ? 'Construction reconnue' : 'Décorticage de l’échantillon')}</span></div>
 <h3>La construction</h3><p>${esc(result.structure || 'La construction complète n’est pas encore couverte. Les éléments ci-dessous constituent une aide partielle.')}</p>
 ${result.literal ? `<div class="analysis-literal"><h3>Dans l’ordre japonais</h3><p class="romaji analysis-romaji">${esc(Romaji.display(row.Romaji))}</p><p class="literal-gloss fr"><em>(${esc(result.literal)})</em></p><p class="muted">${result.literalOrigin === 'automatic' ? 'Lecture indicative assemblée à partir des règles et du dictionnaire. ' : ''}Cette lecture suit les mots et les groupes japonais. Les crochets précisent leur rôle ou leur forme ; ce n’est pas une traduction française naturelle.</p></div>` : ''}
 <p class="muted">Les groupes se lisent dans l’ordre japonais. Touchez une ligne de kana pour l’écouter.</p>
 <ol class="analysis-parts">${result.segments.map(part => `<li class="analysis-part">
 ${block({jp:part.jp,kana:part.kana,audioKana:part.audioKana,romaji:part.romaji,fr:part.fr},{audio:false})}
 ${part.explanation ? `<p>${esc(part.explanation)}</p>` : ''}
 ${part.baseWord ? `<details><summary>Forme de dictionnaire</summary>${block({jp:part.baseWord.mot,kana:part.baseWord.kana,romaji:part.baseWord.romaji,fr:part.baseWord.fr},{audio:false})}</details>` : ''}
 ${part.wordId ? `<a class="analysis-word-link" href="#dictionnaire/${esc(part.wordId)}">Voir dans le dictionnaire →</a>` : ''}
 </li>`).join('')}</ol>
 ${result.note ? `<p class="note">${esc(result.note)}</p>` : ''}
 <div class="fr"><h3>Le sens dans le dialogue</h3><p>${esc(row.Français)}</p></div>
 <details class="analysis-method"><summary>Comment ce décorticage est obtenu</summary>
 <p>Le calcul se fait dans votre navigateur, sans appel à une IA. La traduction vient de la leçon. ${result.literalOrigin === 'automatic' ? 'La lecture dans l’ordre japonais est assemblée automatiquement ; certains sens et rôles demandent encore le contexte.' : 'La lecture dans l’ordre japonais a été préparée pour cet exemple.'}</p>
 <ul>${Object.entries(result.counts).filter(([,n])=>n).map(([key,n])=>`<li>${labels[key]} : ${n} groupe${n>1?'s':''}.</li>`).join('')}</ul>
 <p>Construction : ${result.structureOrigin === 'rule' ? 'modèle reconnu par une règle.' : result.structureOrigin === 'annotation' ? 'annotation préparée pour cette phrase.' : 'non couverte.'} Reconnaître les mots ne garantit pas que toute la construction soit comprise.</p>
 ${decorticageData.sources.map(source=>`<p><a href="${esc(source.url)}" target="_blank" rel="noopener">${esc(source.title)}</a></p>`).join('')}</details>`;
}
document.addEventListener('toggle', event => {
 const panel = event.target;
 if (!panel.matches?.('[data-analysis]') || !panel.open || panel.dataset.loaded) return;
 const row = lessons.find(row => `${row.Leçon}-${row.Ligne}` === panel.dataset.analysis);
 if (!row) return;
 panel.querySelector('.analysis-content').innerHTML = renderDecorticage(row);
 panel.dataset.loaded = 'true';
}, true);

function lessonGrammarLinks(related){
 const link=item=>`<div class="lesson-grammar-link"><a href="#grammaire/${item.card.id}">${esc(item.card.titre)} →</a>${item.row?`<a class="grammar-evidence" href="#lecons/${item.row.Leçon}/${item.row.Ligne}">Repérer dans la phrase ${item.row.Ligne.slice(1).replace(/^0/,'')} ↗</a>`:''}</div>`;
 if(!related.length)return '<p class="muted">Aucune correspondance repérée parmi les 20 fiches actuelles.</p><a href="#grammaire">Parcourir les fiches de grammaire →</a>';
 return related.slice(0,4).map(link).join('')+(related.length>4?`<details><summary>${related.length-4} autres points de grammaire</summary>${related.slice(4).map(link).join('')}</details>`:'');
}

function lessonLabel(id){
 const title=lessons.find(row=>row.Leçon===id&&row.Ligne==='S00')?.Français;
 return `Leçon ${id.slice(1)} — ${title || 'Sans titre'}`;
}
